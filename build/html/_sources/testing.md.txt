Testing
============

To run the tests for the `django-currency-updater` library, you can use Django's built-in test framework. 

## Running Tests
To run the tests, simply run the following command:

```bash
python manage.py test django_currency_updater 
```
